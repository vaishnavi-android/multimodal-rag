# --- Core / config -----------------------------------------------------
python-dotenv==1.0.1

# --- Ingestion: PDF / image / OCR / tables ------------------------------
pymupdf==1.24.10          # PDF text + rendering
pdfplumber==0.11.4        # table extraction
Pillow==10.4.0            # image handling

# --- OCR ---------------------------------------------------------------
rapidocr
onnxruntime

# --- Embeddings ----------------------------------------------------------
sentence-transformers==3.1.1

# --- Vector store (Phase 4: Chroma; swap later for qdrant-client) -------
chromadb==0.5.5

# --- LLM (Ollama) ----------------------------------------------------------
requests==2.32.3

# --- API (Phase 7) -------------------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.31.0
pydantic==2.9.2

# --- UI (Phase 9) ----------------------------------------------------------
streamlit==1.39.0

# --- Testing ---------------------------------------------------------------
pytest==8.3.3
httpx==0.27.2              # FastAPI test client dependency
